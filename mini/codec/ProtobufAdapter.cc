#include "mini/codec/ProtobufAdapter.h"

// Intentionally empty translation unit.
// Template implementations are provided in header to keep concrete message types
// instantiation flexible at call sites.
